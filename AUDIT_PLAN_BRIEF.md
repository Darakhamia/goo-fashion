# Бриф агента: работа по плану аудита

Этот файл — рабочий протокол для любого AI-агента (Claude), который берёт задачи из [`AUDIT_DEV_PLAN.md`](./AUDIT_DEV_PLAN.md). Прочитай его целиком до первой правки кода.

Здесь три вещи: **стартовый промт** (копировать в новую сессию), **правила работы** и **доска статусов** (её агент обновляет по мере закрытия задач).

---

## 1. Стартовый промт

> Копировать блок целиком в новую сессию Claude. Больше ничего писать не нужно — остальное агент прочитает сам.

```
Ты работаешь над продуктом Goo Fashion (Next.js 16 / React 19 / Clerk / Supabase / Replicate / monobank).

Твоя работа ведётся строго по плану, составленному по итогам аудита сайта от 7 августа 2026.

Сделай по порядку:
1. Прочитай AUDIT_PLAN_BRIEF.md — это протокол работы. Следуй ему без исключений.
2. Прочитай AUDIT_DEV_PLAN.md — это единственный источник истины по задачам.
3. Открой доску статусов в конце AUDIT_PLAN_BRIEF.md и возьми первую незакрытую задачу
   по порядку волн. Не выбирай задачу «поинтереснее» и не перепрыгивай волну.
4. Перед правкой перепроверь по коду, что проблема ещё существует: план написан
   7 августа 2026, часть задач могла быть уже закрыта другим агентом или человеком.
5. Сделай задачу, проверь её по DoD из плана фактическим прогоном, обнови доску
   статусов, закоммить.

Жёсткие правила:
— Не расширяй объём задачи и не делай рефакторинг «заодно».
— Не принимай за человека решения из раздела «Решения, которые ты не принимаешь».
— Не отмечай задачу закрытой, пока DoD не проверен фактически. Не «должно работать» — проверено.
— Если план разошёлся с кодом, доверяй коду и напиши об этом в отчёте.

Когда закрыта последняя задача волны — остановись и подготовь отчёт для CEO по форме
из раздела «Эскалация к CEO». Новый план ты не пишешь: его составляет CEO.
```

---

## 2. Роль и рамки

Ты — исполнитель по готовому плану, а не автор стратегии.

**Что делаешь ты:** берёшь задачу из плана, проверяешь актуальность, выполняешь, доказываешь результат по DoD, отмечаешь на доске, коммитишь.

**Что делает CEO:** приоритеты, продуктовые и деньговые решения, новый план после закрытия волны.

Контекст, из-за которого этот план вообще существует: у продукта **ноль платящих пользователей**, потому что кнопка оплаты вела на несуществующий маршрут. Всё в плане отсортировано от «мешает получить деньги» к «помогает расти». Порядок — не рекомендация, а требование.

---

## 3. Источник истины

| Вопрос | Где ответ |
|---|---|
| Что делать и в каком порядке | `AUDIT_DEV_PLAN.md` |
| Как работать, что можно и нельзя | этот файл |
| Что уже закрыто | доска статусов, раздел 8 |
| Устройство кодовой базы | `graphify query "<вопрос>"`, затем `graphify-out/GRAPH_REPORT.md` |
| Политика веток и коммитов | `CLAUDE.md` |

Если инструкции твоей сессии противоречат `CLAUDE.md` по веткам — приоритет у инструкций сессии.

---

## 4. Порядок работы

### 4.1. Волны идут подряд

```
Волна 1  Б0 целиком + Б5-1            → сайт можно купить
Волна 2  Б1, Б2, Б3, Б4, Б5           → платное действительно платное, воронка измеряется
Волна 3  Б6, Б7, Б9, Б10              → трафик приходит, понимает продукт, возвращается
Волна 4  Б8, Б11                      → каталог и решение по модели монетизации
```

Волна не начинается, пока предыдущая не закрыта полностью. Единственное исключение уже учтено в плане: **Б5-1** (открытый `/api/report-bug`) вынесен в Волну 1, потому что это дыра в расходах.

Внутри волны порядок блоков — по номеру, внутри блока — по номеру задачи.

### 4.2. Цикл одной задачи

1. **Взять** первую незакрытую задачу с доски.
2. **Перепроверить актуальность.** План написан 7 августа 2026. Открой указанные в задаче файлы и убедись, что проблема на месте. Если её уже нет — отметь `н/а` с доказательством и переходи к следующей. Это не пропуск, это нормальный исход.
3. **Сделать** ровно то, что описано.
4. **Проверить по DoD.** DoD в плане сформулирован проверяемо — выполни проверку буквально. Если DoD требует живого прогона (пройти воронку оплаты, получить 429, увидеть событие в аналитике) — прогони, а не рассуждай.
5. **Обновить доску** в разделе 8: статус, дата, одна строка доказательства.
6. **Закоммитить** с ID задачи в первой строке сообщения: `Б2-3: месячная квота генераций изображений`.
7. **Запустить** `graphify update .`, если менял код.

### 4.3. Одна задача — один коммит

Не смешивай задачи в одном коммите. Если по ходу нашёл посторонний баг — не чини молча: запиши его в раздел 7 «Найдено сверх плана» и продолжай свою задачу.

---

## 5. Жёсткие правила

**Не расширяй объём.** Задача `Б0-1` — заменить две строки. Не переписывай попутно страницу подписки, даже если видно, как сделать лучше.

**Доверяй коду, а не плану.** План — снимок на 7 августа 2026. Разошлось — прав код. Напиши о расхождении в отчёте CEO, чтобы следующий план был точнее.

**DoD проверяется, а не предполагается.** «Логика верная, должно работать» — не закрытая задача. Нужен факт: прошёл путь, увидел ответ, получил письмо, событие появилось в дашборде.

**Отчитывайся честно.** Не получилось — так и скажи, с причиной. Сделал половину — скажи, какую. Частично закрытая задача с честной пометкой полезнее, чем закрытая на словах.

**Не трогай то, что аудит признал здоровым.** В `AUDIT_DEV_PLAN.md` есть раздел «На что опираемся — не переделывать»: доступы админки, защита промпта от инъекций, код интеграции monobank, SEO-фундамент, builder, дизайн-система. Если задача требует туда залезть — это повод остановиться и спросить.

**Деньги и данные пользователей — с подтверждением.** Миграции на проде, изменение цен, массовые рассылки, удаление данных, смена лимитов на живых подписках: подготовь, покажи, дождись подтверждения. Не выполняй сам.

---

## 6. Решения, которые ты не принимаешь

Эти развилки закреплены за CEO. Дошёл до них — останавливайся и спрашивай, а не выбирай сам «разумный вариант».

| Где | Развилка |
|---|---|
| **Б0-3** | Чем чинить 429 на изображениях: поднять тариф Vercel, свой ресайз при импорте или гибрид |
| **Б2-1** | Размер бесплатной воронки стилиста (в плане предложено 5 сообщений без регистрации / 15 после — это предложение, не решение) |
| **Б2-5** | Реализовать `stylistMemory` и `exclusiveStyles` или убрать их из тарифа |
| **Б3-1** | По каждой строке `/plans`: реализовать обещание или переписать страницу |
| **Б3-3** | Сетка тарифов: оставить три ступени или свести к одной $6–8 |
| **Б8-2** | Источники и объём женского сегмента |
| **Б8-5** | Ценовое позиционирование каталога ($1,19 — $8 506 в одной ленте) |
| **Б11** | Подписка или партнёрка как основная модель |

Формат вопроса к CEO: что за развилка, варианты, чем отличаются по деньгам и срокам, твоя рекомендация одной строкой. Не разворачивай в эссе.

---

## 7. Найдено сверх плана

Сюда агенты дописывают то, что нашли по ходу, но чинить не стали. Это вход для следующего плана.

| Дата | Что нашли | Где | Насколько срочно |
|---|---|---|---|
| 2026-08-07 | 429 на изображениях — не исчерпанная квота, а один хост. Farfetch 6/6 → 429, productserve и Supabase 6/6 → 200, включая заведомо некешированный `w=3840`. Тот же файл напрямую с CDN Farfetch — 200. Значит вариант «поднять тариф Vercel» из Б0-3 денег стоит, а проблему не решает | замер по проду, `/_next/image` | Входит в Б0-3, решение за CEO |
| 2026-08-07 | `auth.protect()` в `src/proxy.ts` уводит на дефолтный Clerk-хост, а не на `/login` — это второй вход в аккаунт, о котором пишет Б7-5. `NEXT_PUBLIC_CLERK_SIGN_IN_URL` в `.env.example` не заведён | `src/proxy.ts:71` | Не срочно — уже учтено в Б7-5 |
| 2026-08-07 | Рейт-лимиты работают только при заданных `UPSTASH_REDIS_REST_URL/TOKEN`: без них `runLimit` пропускает все запросы. Ограничение из Б5-1 (5 репортов в час) в проде существует только если Upstash подключён | `src/lib/server/rate-limit.ts:61` | Проверить вместе с Б0-5 — тот же поход в переменные Vercel |
| 2026-08-07 | `graphify` CLI в контейнере не установлен (`npx graphify` → «could not determine executable»), поэтому шаг 4.2.7 протокола не выполнялся. Ориентировка шла по `graphify-out/GRAPH_REPORT.md` | окружение | Низкая, но граф после этих коммитов устарел |
| 2026-08-10 | Нет баннера согласия на куки, при этом PostHog (если задан `NEXT_PUBLIC_POSTHOG_KEY`) ставит аналитическую куку без opt-in — риск по ePrivacy/GDPR для посетителей из ЕС. Обновлённая Cookie Policy обещает согласие «where required by law» | `src/app/layout.tsx:95`, `src/components/analytics/PostHogTracker.tsx:27` | Закрыто 2026-08-10 решением CEO: добавлен баннер согласия (`src/components/consent/CookieConsentBanner.tsx`), PostHog грузится только после «Accept», выбор меняется на `/cookie`. Тем же решением с юридических страниц убран почтовый адрес — оставлен город и «по запросу» |
| 2026-08-10 | `/api/upload` без авторизации пишет любые файлы ≤10 МБ в публичный бакет `outfit-images` (middleware не защищает `/api/*`) — риск злоупотребления хранилищем | `src/app/api/upload/route.ts:32-55`, `src/proxy.ts:13-17` | Высокая для Б5 (безопасность), фикс — одна проверка auth |
| 2026-08-10 | Самостоятельного удаления аккаунта и экспорта данных нет; админское удаление стирает только Clerk-пользователя, строки в Supabase (`user_likes`, `user_looks`, `pending_looks`, `stylist_chats`) остаются сиротами. Privacy Policy обещает удаление по письму — процесс ручной | `src/app/api/admin/users/[id]/route.ts:261` | Средняя: нужен скрипт/эндпоинт полного удаления |
| 2026-08-10 | Аналитические таблицы (`page_views`, `web_vitals`, `analytics_events`) не чистятся — ретеншена нет вообще, строки копятся бессрочно | `supabase/migrations/001_analytics.sql` | Низкая сейчас, вырастет с трафиком |
| 2026-08-10 | Сгенерированные try-on изображения (могут изображать пользователя) лежат в публичном бакете `generated-outfits` — доступны любому, у кого есть URL. В Privacy Policy это раскрыто честно, но лучше приватный бакет + подписанные URL | `src/lib/storage.ts:12-14,44-57` | Средняя, приватность пользователей |
| 2026-08-10 | Админская рассылка через Resend не содержит ссылки отписки (только текст «you have an account») — риск по антиспам-нормам; в Privacy Policy заявлено право возразить по email | `src/app/api/admin/email/route.ts:83` | Низкая, пока рассылка не используется массово |
| 2026-08-10 | **Поиск товаров не умел фильтровать по полу** — блокер для Б8-2. `match_products` (007) и `search_products` (006) принимали только `filter_category` и `max_price_usd`; `filter_gender` не встречался в репозитории ни разу. Незаметно, пока каталог 314 мужских против 3 женских, но в момент заливки женского фида стилист начал бы предлагать мужчинам платья. **Починено** по прямому указанию CEO: миграция 014 + все 4 пути выдачи в `stylist/chat`. Проверено на живом Postgres 16 + PostgREST, 14/14 | `supabase/migrations/014`, `api/stylist/chat/route.ts` | Закрыто, но Б8-2 без этого заливать было нельзя |
| 2026-08-11 | **`npm run dev` не поднимается вообще.** Tailwind v4 сканирует `.md` в корне, и строка-пример `text-[var(--fg-on-dark-60/70/80)]` из `DESIGN_SYSTEM.md:78` попадает в него как кандидат класса → генерируется `color: var(--fg-on-dark-60/70/80)` → dev-загрузчик CSS падает с `Unexpected token Delim('/')`, каждый маршрут отдаёт 500. `npm run build` при этом exit 0: продовый оптимизатор ту же строку роняет с предупреждением («Found 1 warning while optimizing generated CSS»), а не с ошибкой. То есть прод здоров, а локальная разработка невозможна. Починка — снять из документации форму записи со слешем (или закрыть `.md` от сканера через `@source`) | `DESIGN_SYSTEM.md:78` (симптом — `src/app/globals.css`) | Средняя: прод не затронут, но dev-сервер нерабочий у всех |
| 2026-08-11 | Оба auth-маршрута — Clerk-catch-all, а `ConditionalSiteLayout` сравнивал путь на равенство и знал только `/login`. Значит `/register` рисовался внутри хрома сайта, а `/login` теряла «bare» со второго шага (`/login/factor-one`, `/login/reset-password-*`). **Починено** в этой же задаче по прямому указанию CEO (редизайн входа/регистрации): совпадение переведено на префикс и добавлен `/register`. Проверено живыми запросами: на `/login`, `/register`, `/login/factor-one`, `/register/verify-email-address` в HTML ноль маркеров хрома, на `/about` они есть | `src/components/layout/ConditionalSiteLayout.tsx:31` | Закрыто |
| 2026-09-05 | **403 от магазинов: парсер ходил только за HTML.** «Свой скрейпер» — это не отдельный сервис, а `src/lib/server/parser/*` в режиме `direct` (коммит `c8d2294` от 2026-08-10); текст ошибки на скриншоте CEO — ветка `settings.provider === "direct"` в `crawl.ts`, то есть провайдер не настроен. Обходить Cloudflare из Vercel-функции нельзя в принципе (JA3, JS-челлендж, IP дата-центра), поэтому **по прямому указанию CEO** сделан вариант «не ходить в HTML»: Shopify storefront JSON (`/products/<handle>.json`, `/collections/<h>/products.json`) — вся галерея, варианты, цвет и размеры данными, и обычно 200 там, где страница 403; сайтмап как запасной путь для заблокированного листинга; один ретрай на 429/503 по `Retry-After` и на 403 со сменой профиля; паузы 150–400 мс между запросами; `{impersonate}`/`{timeout}` доехали до custom-эндпоинта. Доказательство: 31/31 на фиктивном магазине (Shopify с 403 на HTML → 4 фото, цвет, размеры, валюта из `/meta.json`, HTML не запрашивался ни разу; не-Shopify с 403 → 3 товара из сайтмапа, блог-сайтмап не открыт; ретраи 429/403/404; подстановка плейсхолдеров). `tsc` чист, `npm run build` exit 0 | `src/lib/server/parser/{shopify,sitemap,crawl,fetch,parse-page}.ts` | Закрыто |
| 2026-09-03 | **Парсер забирал одну фотографию вместо галереи и не заполнял цвет.** Проверено по коду и фикстурам: `upgradeImageUrl` отбрасывал любой URL без расширения (Scene7, Zara, imgix — там терялась и галерея, и главное фото), `harvestGalleryImages` возвращал пустой список, если структурные данные не дали ни одной опорной картинки, а `raw.color` читался только из JSON-LD `color` и рецепта — ни meta, ни microdata, ни разметки, ни названия. `color_group_ids` при импорте не выставлялся вообще, поэтому спарсенный товар не попадал ни в один цветовой фильтр. **Починено по прямому указанию CEO** (вне порядка волн): четыре признака родства фото + разбор безрасширенных CDN, цвет из всех слоёв разметки с запасным выводом из названия и слага, канонизация колорвеев («Core Black» → black) и запись `colors`/`color_hex`/`color_group_ids` на импорте. Доказательство: 35/35 фикстур парсера (Shopify, Farfetch, Scene7, магазин без структурных данных, Cuyana как ловушка на ложные срабатывания), 10/10 на слиянии AI-полей и 10/10 на живом прогоне `importParsedProduct` через настоящий `supabase-js` против заглушки PostgREST, включая базу без колонки `color_group_ids`. `tsc --noEmit` чист, `npm run build` exit 0 | `src/lib/server/parser/{gallery,extract,normalize,ai-extract,import-product}.ts`, `src/lib/server/product-fields.ts` | Закрыто |
| 2026-09-12 | **Жук умел спрашивать данными только Shopify.** Проверено по коду: путь «данные вместо страницы» существовал лишь для Shopify (`parse-page.ts:106`, `crawl.ts:249`), поэтому Woo- и Squarespace-магазины шли через HTML — платили модельным вызовом за отсутствие structured data, а при 403 не отдавали ничего вообще. **Сделано по прямому указанию CEO** (шаг 1 бесплатного плана, деньги не задействованы): WooCommerce Store API (`?slug=`, `?category=<id>`, `per_page=100`, две базы — `/v1` и старая, цена в минорных единицах) и Squarespace `?format=json` (payload сам говорит, товар это или листинг; пагинация по его же `nextPageUrl`); обе витрины спрятаны за одной дверью `storefront.ts` с памятью «не эта платформа» на хост и общим потолком 12 с на все пробы. Заодно починен приём сигнала платформы: `getStoreJson` выбрасывал тело любого не-2xx, а WordPress отвечает «маршрута нет» именно 404 с `{"code":"rest_no_route"}` — без этого Woo со старой базой не находился. Доказательство: 60/60 проверок на пяти фиктивных магазинах через реальный HTTP и реальный `direct`-fetch (Woo, Woo со старой базой, Squarespace, Shopify, «ни один из трёх»): при 403 на всех HTML-путях товар всё равно собран — 3 фото, цена 42.00 из `"4200"` + `currency_minor_unit`, зачёркнутая 52.00, цвет из `pa_colour`, размеры S/M/L; каталог 3 товара, категория ровно 2 (slug переведён в id), неизвестная категория отдаёт ноль, а не весь магазин; Squarespace: цена со скидкой 75/90 GBP, галерея дедуплицирована, товарный URL отвечает одним товаром, блог-запись товаром не считается; на «чужом» магазине одна проба на платформу и ноль проб со второго раза, HTML-путь и его подсказка не тронуты. `tsc --noEmit` чист, `npm run build` exit 0, `eslint src/lib/server/parser/` чист | `src/lib/server/parser/{storefront,store-json,woocommerce,squarespace,parse-page,crawl}.ts` | Шаг 1 из четырёх бесплатных; следующие — сайтмап (gzip, больше имён), формы товарных URL, мягкие стены (cookies + порядок шифров) |
| 2026-08-10 | При добавлении параметра `create or replace function` не заменяет функцию, а создаёт **перегрузку**: PostgREST потом не может выбрать кандидата и падает с `function search_products(...) is not unique` на каждом существующем вызове. Поймано живым прогоном; в миграции 014 добавлены явные `drop function`. Тот же капкан ждёт любую будущую миграцию, меняющую сигнатуру RPC | `supabase/migrations/*` | Знать на будущее |
| 2026-09-16 | **Отказ магазина в «Collect catalog» вёл в тупик.** По скриншоту CEO: на карточку товара магазин ответил 403, а подсказка отправляла в другую вкладку («use the "Paste page" panel in the Parse URL tab»), где адрес надо вводить заново, а панель вставки свёрнута и ничем себя не выдаёт. Сам путь через жука при этом рабочий — проверено. **Сделано по прямому указанию CEO** (вне порядка волн, денег не задействовано): `discoverProductUrls` при отказе больше не теряет признак «это одна вещь» (`isSingleProduct: startIsProduct`); в красной плашке Collect появилась кнопка «Paste page instead» — переносит адрес во вкладку Parse URL и открывает панель вставки; там же панель открывается сама, когда парс упёрся в блок. Доказательство: 28/28 сквозной прогон жука (магазин отвечает 403 на HTML, сайтмап и витринный JSON; страница открыта в настоящем Chromium, выполнен ровно тот `javascript:`-код, что уходит в закладку; 123 КБ → 1 КБ payload, JSON-LD цел, скрипты/стили/svg/iframe/data-URI сняты, лениво подгруженная галерея приехала отрисованной; `parsePage` собрал товар — 4 фото, 1290 EUR, цвет, бренд — при нуле запросов в магазин) и 12/12 на самом экране в jsdom (кнопка появляется только на отказе карточки товара, адрес переносится, панель открыта, вставка уходит в `/parser/parse` вместе с разметкой; ручное переключение вкладок чужой адрес не тянет). `tsc --noEmit` чист, `eslint` по обоим файлам чист, `npm run build` exit 0 | `src/lib/server/parser/crawl.ts`, `src/app/goo-studio/parser/page.tsx` | Закрыто. Каталог с защищённого магазина по-прежнему требует провайдера либо оставшихся бесплатных шагов |
| 2026-09-16 | **Дверь из отказа открывалась не на каждом отказе.** CEO прислал вторую плашку: текст уже новый («press "Paste page instead" below»), кнопки под ним нет. Воспроизвести на коде не удалось — при таком тексте сервер обязательно отдаёт `isSingleProduct: true` и `status: 403`, то есть на том экране крутился клиент старее ответа (вкладка, открытая до выката). Но проверка обнажила настоящую узость: кнопка показывалась только при `isSingleProduct`, а он ставится по `looksLikeProductPath` — магазин с незнакомой формой адреса (`/shop/12345`) получал отказ вообще без выхода, хотя вставка страницы там работает ровно так же. **Сделано по прямому указанию CEO:** предложение теперь висит на самом факте отказа (401/403/429/503, с чтением текста ошибки, когда поля `status` в ответе нет), а обе оставшиеся подсказки `diagnoseFailedFetch` тоже называют эту дверь. Доказательство: 14/14 на экране в jsdom — кнопка есть на 403 с опознанным товаром, на 403 с неопознанным адресом, на ответе без `status`, на 401 и 429; её нет на 404 и на таймауте (`Timed out after 20403ms` цифрами не обманывает); адрес переносится, панель открыта, вставка уходит в `/parser/parse` с разметкой. `tsc --noEmit` чист, `eslint` чист, `npm run build` exit 0 | `src/app/goo-studio/parser/page.tsx`, `src/lib/server/parser/crawl.ts` | Закрыто. Если кнопки нет и после жёсткого обновления — нужен ответ `/api/admin/parser/crawl` из DevTools |
| 2026-09-16 | Третья плашка от CEO — снова без кнопки. Собран сквозной прогон «настоящий сервер → настоящий экран» (раньше половинки проверялись порознь): `discoverProductUrls` реально ходит в магазин, отвечающий 403, его ответ сериализуется как в роуте и в таком виде отдаётся настоящему компоненту страницы. На коде **из `master` (#854, то, что на проде)** ответ — `{ok:false, status:403, isSingleProduct:true, error:"Upstream responded 403"}`, и кнопка рисуется. Значит серверная часть на проде здорова, а в браузере крутится клиент старее ответа, который он показывает (вкладка, открытая до выката; service worker в проекте отсутствует, своих cache-заголовков нет — лечится перезагрузкой или приватным окном). Чтобы текст подсказки не отправлял к кнопке, которой на старом бандле нет, обе ветки отказа теперь называют и панель: «Paste page» во вкладке Parse URL | `src/lib/server/parser/crawl.ts` | Знать на будущее: подсказка, ссылающаяся на элемент UI, живёт дольше, чем сборка этого UI |
| 2026-09-16 | Родство фотографий в галерее не работает на коротких именах файлов: `stem()` оставляет только буквы и цифры, а `isNumberedFrame` требует восьми символов — поэтому `nebula-1.jpg` / `nebula-2.jpg` (7 символов) сиблингами не считаются, и со страницы уезжает одно фото из четырёх. Для магазина, который называет файлы `1.jpg`, `2.jpg`, галерея теряется вся. Порог осознанный (чужое фото в каталоге хуже пропущенного), поэтому не тронут | `src/lib/server/parser/gallery.ts:200` | Низкая: на реальных именах (`nebula-jacket-aurelio-01.jpg`) галерея собирается полностью — проверено |
| 2026-09-20 | **Жук доставал не весь каталог, а его верхушку.** Проверено по коду: `sitemap.ts` знал три имени и не умел `.gz` (каталог любого размера отдаёт сайтмап сжатым, `fetchHtml` возвращал бы его декодированной кашей), считал документом и 404-й промах (пять «документов» кончались на трёх неверных догадках), открывал максимум три дочерних сайтмапа и читал `<loc>` только без `CDATA` — у WordPress и Magento сайтмап поэтому читался пустым; `looksLikeProductPath` требовал сегмента `/product(s)/` или цифрового кода, поэтому магазин со slug-адресами (`/shop/nebula-jacket-aurelio`) давал «сайтмап читается, товаров 0»; сайтмап вообще читался только при полностью пустом обходе, а не при коротком; потолки — 500 товаров в `crawl.ts`, 60 якорей на страницу в `extractProductLinks`, 40 товаров и 1 страница по умолчанию на экране; в `direct` не было cookie-jar, то есть каждый запрос к мягкой стене был тем самым первым, который она и отклоняет. **Сделано по прямому указанию CEO** (блок «добыча», шаги 2–4 бесплатного плана; деньги не задействованы, провайдер по решению CEO не берём): семь имён сайтмапа + три `.gz` через новый `fetchBinary` с распознаванием по магическим байтам (только `direct` — провайдер отдаёт текст), `CDATA` в `<loc>`, раздельные бюджеты (18 запросов / 12 прочитанных документов / 10 детей), файл, который магазин сам назвал `product-*`, считается его собственным словом — его URL минуют тест формы адреса, но не плоские отказы (`/cart`); четвёртая форма товарного адреса (секция магазина + слаг из трёх слов, с отсечкой листинговых слов и родителей вида `/collections/<slug>`); сайтмап теперь добирает недобранное, а не только спасает пустое; потолки 2 000 товаров, вся сетка со страницы листинга, по умолчанию 100/3 страницы; cookie-jar на хост (10 минут, `direct`), стук во входную дверь на молчаливом 403 — один раз на хост и только когда сам отказ куку не выдал. **Доказательство:** 51/51 на пяти фиктивных магазинах через настоящий HTTP и настоящий `direct`-fetch (имена ведут на 127.0.0.1 через `/etc/hosts`, поэтому SSRF-гард видит публичный хост и путь кода не подменён): gzip-сайтмап найден после шести мёртвых имён (3 товара, ≤18 запросов); robots → индекс с `CDATA` → `product-sitemap.xml` даёт 3 вещи, включая короткий слаг `/shop/tee`, `/cart` отброшен, `page-sitemap.xml` не открыт; мягкая стена с кукой в самом отказе — ровно один повтор, во входную дверь не стучались; стена с кукой только на главной — 403, один стук в `/`, повтор с кукой прошёл; вторая страница того же магазина прошла с первого раза одним запросом; каталог 1 500 URL собран целиком (потолок 500 снят), якоря листинга внутри, подсказка говорит «added»; сетка из 120 карточек отдаёт 120 при лимите и 60 по умолчанию; магазин, отказывающий во всём, по-прежнему отвечает 403 с подсказкой про Paste page; вставленная одна вещь в slug-форме опознана как товар и сайтмап не читался. `tsc --noEmit` чист, `eslint` по парсеру, роутам и экрану чист, `npm run build` exit 0 | `src/lib/server/parser/{sitemap,fetch,extract,crawl}.ts`, `src/app/api/admin/parser/crawl/route.ts`, `src/app/goo-studio/parser/page.tsx`, `PARSER.md` | Закрыто. Осталось от бесплатного плана: порядок шифров/JA3 (в Node не воспроизводится — нужен свой curl_cffi, решение за CEO). Блоки B (наличие, SKU, размеры, история цен) и C (очередь, возобновление, расписание) не начаты |
| 2026-09-21 | **Каталог с защищённого магазина: руки перенесены в браузер CEO, мозги остались на сервере.** Продолжение блока «добыча» (2026-09-20): последний оставшийся бесплатный шаг — порядок шифров/JA3 — в Node не воспроизводится, поэтому **по прямому указанию CEO** (вне порядка волн, деньги не задействованы) задача обойдена целиком: страницы открывает расширение Chrome на машине CEO (его адрес, его куки, уже пройденная проверка), а что открывать и как быстро — решает сервер. Новое: `robots.ts` — разбор robots.txt строго по группе `*` (Googlebot'ом не представляемся), Allow/Disallow по длиннейшему совпадению с `*` и `$`, Crawl-delay, Sitemap, разбор чистый (сеть не трогает); `plan-collection.ts` — чистая функция «страница + сайтмапы + robots → товарные адреса, пауза, что дочитать следующим кругом» поверх существующих `extractProductLinks`/`looksLikeProductPath`/`isNonProductPath` и хелперов `sitemap.ts` (они для этого экспортированы, ни одной копии не заведено; `fromRobots` переведён на общий разбор, дубль исчез); роут `collect` с действиями `plan` и `ingest` (`ingest` зовёт `parsePage` с готовым html — в магазин не ходит вовсе, без html отвечает 400, а не молча фетчит); экран-приёмник `/goo-studio/parser/collect` — мост postMessage ↔ API обычной сессией Clerk; `extension/` (MV3): popup, service worker с очередью и темпом, content script-санитайзер один в один с букмарклетом, bridge. **Ключей в расширении нет** — оно не умеет звать API, только просить открытую вкладку; доступ к магазину через `optional_host_permissions` + запрос по клику. Вежливость: Disallow соблюдается, пауза `max(Crawl-delay, 1.5 с)` + случайный разброс, ~10 с отдыха каждые 20 страниц, два 403/429 подряд — остановка с объяснением, Stop мгновенный (гасит и текущую паузу, и приёмник перестаёт принимать). **Доказательство:** 39/39 на настоящих модулях `robots.ts`/`plan-collection.ts` (длиннейшее совпадение, ничья в пользу Allow, пустой Disallow, `*` и `$`, слитые группы агентов, robots.txt отданный HTML-страницей, пол и потолок паузы, дедуп по `seen`, индекс → дети) и 27/27 сквозным прогоном настоящего расширения в настоящем Chromium против фиктивного магазина: при установке не выдан ни один хост (`explicit_host: []`, скриптуется только собственный экран приёмника, из API — `activeTab`, `scripting`, `storage`, `webRequest`); robots.txt прочитан первым, сайтмап вторым кругом; запрещённый `/product/secret-*` не открыт ни разу и посчитан как blocked; темп 2 с взят из Crawl-delay магазина, разброс между запросами есть; страница ~430 КБ ужата до <300 КБ, JSON-LD цел, лениво подгруженная галерея приехала отрисованной (2 настоящих фото вместо data-URI), настоящий `parsePage` собрал товар из снятого DOM (имя, бренд Fixture Atelier, 249 EUR) при нуле запросов в магазин с нашей стороны; на 403 прогон встал ровно после второй страницы, не третьей; после Stop ни одного импорта и ни одного запроса в магазин; на 21 товаре без Crawl-delay применён пол 1.5 с и после двадцатой страницы лёг отдых 11 с. `tsc --noEmit` чист, `npm run build` exit 0, `eslint` по новым файлам и по `extension/` чист, база линта не выросла (13 ошибок / 52 предупреждения до и после — все чужие, по моим файлам ноль) | `src/lib/server/parser/{robots,plan-collection,sitemap}.ts`, `src/lib/server/audit.ts`, `src/app/api/admin/parser/collect/route.ts`, `src/app/goo-studio/parser/collect/page.tsx`, `extension/*` | Закрыто в коде. **Не проверено мной:** живой магазин и установка в Chrome — из контейнера это серверный IP, то есть ровно тот, от которого уходим; проверку на настоящем магазине делает CEO |
| 2026-09-22 | **Все фотографии вещи и цена в одной валюте.** Три правки по прямому указанию CEO (вне порядка волн), все — поверх сбора расширением от 2026-09-21. **Почему Farfetch отдавал меньше фото, чем остальные** — найдено по коду, а не угадано: `collectCandidates` в `gallery.ts` добывает адреса из инлайновых hydration-payload'ов, но `extension/snapshot.js` удаляет все `<script>` кроме `ld+json` **до** отправки, поэтому на Shopify (галерея в разметке) не терялось ничего, а на SPA вроде Farfetch до сервера доезжали только те слайды, что успела смонтировать карусель. Вторая, более широкая дыра рядом: регулярка payload'а разрешала `\/` лишь сразу после схемы, а внутри пути упиралась в обратный слэш — то есть адрес вида `https:\/\/cdn.shopify.com\/s\/files\/1\/photo.jpg` (так payload, закодированный в JSON дважды, пишет любой адрес) не находился вообще: схема совпадала, до расширения файла дело не доходило. **Сделано.** Фото: `snapshot.js` читает страницу, пока она цела (`currentSrc`, `src`, `srcset`/`data-srcset`, зум-атрибуты, `picture>source`, `link[as=image]`, плюс скан содержимого `<script>` на адреса картинок) и отдаёт найденное как кандидатов вместе с видимым текстом цены — сами payload'ы по-прежнему не отправляются, иначе вернулись бы те мегабайты, ради которых strip и существует; кандидаты проходят **тот же** фильтр `harvestGalleryImages` (новый пятый параметр), так что рельс «you may also like» отбрасывается, как и раньше; новый `PageEvidence` в `types.ts` прокинут `route → parsePage → extractProduct`; регулярка экранированных слэшей исправлена в обоих местах (сервер и расширение); потолок фото 12 → 20 одной общей `MAX_PRODUCT_IMAGES` — до этого три места расходились (нормализация 12, импорт 12, зеркало жёстко `min(max, 12)`, то есть найденное сверх 12 хранилось ссылкой и не скачивалось никогда). Валюта: новый `src/lib/server/fx.ts` (таблица курсов с базой USD, кэш на час, кэшируется и неудача — провайдер, который лёг, лежит не для одного товара; фолбэк-таблица на 31 валюту), `/api/exchange-rates` переведён на него и больше не дублирует ни фетч, ни фолбэк; импорт пересчитывает цену в доллары и пишет `price_min/price_max` в долларах с `currency: USD`, а цену магазина сохраняет там, где она и есть правда — в записи ритейлера (по ней человек платит) и в новых колонках `source_price/source_currency/fx_rate/fx_date` (миграция 019, добавлены в `OPTIONAL_COLUMNS`, так что база без миграции всё равно принимает товар, просто без происхождения); `normalizeCurrency` больше **не** подставляет USD молча — пустая строка и issue «currency not stated», потому что молчаливый дефолт и есть та причина, по которой ₴4 000 приехали как $4 000; `extractCurrencyFromDisplay` расширен (₴ ₽ ₹ ₩ ₪ zł Kč CHF 元, слова грн/руб/лв, CA$/A$/NZ$/HK$/S$/R$ через lookbehind — иначе «S$» внутри «US$» читался как сингапурский доллар, и код ISO принимается только рядом с числом, иначе «incl. VAT» становится валютой, а «Try it on» — турецкой лирой); экран приёмника показывает по каждой странице «N photos · 4000 UAH → $97.56». **Доказательство.** 47/47 юнитов на настоящих модулях (`verification/test-price-and-photos.js`: символы и слова валют, обе ловушки с долларами, конвертация и один фетч курсов на весь прогон, фолбэк при недоступном провайдере, `null` на валюту без курса, кандидаты против рельса рекомендаций и чужого CDN, экранированные слэши, потолок 20, `productToDb`/`dbToProduct` и сброс четырёх колонок на базе без миграции 019) и 40/40 сквозным прогоном настоящего расширения в настоящем Chromium — было 27/27, добавлен режим витрины `spa`, устроенный как Farfetch: галерея только в `__NEXT_DATA__`, половина адресов с экранированными слэшами, рядом рельс чужого товара на том же CDN. На нём: до правки со страницы уезжало 2 фото, теперь 7 из 7; рельс не попал в товар; payload не отправлен (strip работает, разметка <300 КБ); гривневая страница, у которой в разметке `price: "4000"` вовсе без `priceCurrency`: валюта прочитана из отрисованного «4 000 ₴», ₴4 000 → $97.56 по курсу 41, issue об отсутствии валюты не выставлен; евро 1290 → $1517.65; прогоны A–D (вежливость, Crawl-delay, 403, Stop, отдых) без регрессий. `tsc --noEmit` чист, `npm run build` exit 0, `eslint` по изменённым файлам и по `extension/` чист, база линта не выросла (13 ошибок / 52 предупреждения до и после) | `src/lib/server/fx.ts`, `src/lib/server/product-fields.ts`, `src/lib/server/parser/{gallery,extract,normalize,parse-page,import-product,types}.ts`, `src/lib/server/storage/product-images.ts`, `src/lib/data/db.ts`, `src/lib/{types,supabase}.ts`, `src/app/api/exchange-rates/route.ts`, `src/app/api/admin/parser/collect/route.ts`, `src/app/goo-studio/parser/collect/page.tsx`, `extension/{snapshot,background}.js`, `supabase/migrations/019_product_source_price.sql` | Закрыто в коде. **Требует шага CEO:** миграция 019 (без неё цена всё равно в долларах, но происхождение не пишется). **Не проверено мной:** живой Farfetch и живая база — Supabase в контейнере нет, поэтому запись строки проверена на чистых `productToDb`/`dbToProduct` и на сбросе колонок, а не на реальном insert. **Осталось от прошлого раза:** название товара (og:title тащит имя магазина, `<h1>` парсер не читает вовсе) — CEO перенёс на потом |
| 2026-09-22 | **Десять пустых полей товара.** По прямому указанию CEO (вне порядка волн), шесть частей, каждая с отдельным доказательством. **Найдено по коду:** размеры — разбор JSON-LD **всегда** возвращал `sizes: []`, единственным источником были per-site правила, то есть размеров почти никогда и не было; подкатегорию импортёр **не писал вообще** (`resolveSubcategory` существовал, URL-импорт его не звал); `styleKeywords: []` прошит в импортёре при живом словаре из 13 стилей; группировку цветов CSV-импорт умеет (`getGroupKeys`), URL-импорт не звал; сопоставления одного товара между магазинами не было нигде, `sku`/`gtin` со страницы не читались; материал брался только из JSON-LD `material`; описание — из `og:description`, то есть обрезанной маркетинговой строки; цвет и бренд — только из структурных данных, которых у половины магазинов нет. **Сделано.** Общий принцип: расширение добывает кандидатов с отрисованной страницы, словари и проверки живут на сервере в одном месте. Размеры — четыре формы структурных данных (`size`, `hasVariant`, `offers`+`itemOffered`, `additionalProperty` на шести языках) плюс отрисованный контрол; `looksLikeSize`/`pickSizes` отсеивают «Select size», ссылку на сетку и счётчик количества; первый непустой источник, не слияние (иначе «40», «EU 40» и «IT 40» станут тремя размерами). Цвет — выбранный свотч и строка таблицы характеристик выше скана разметки; фильтр цвета заработал сам, потому что `colorGroupNamesFor` уже раскладывал «Charcoal» → Grey и ждал только названия. Описание — отрисованный блок через `textContent` (у свёрнутого аккордеона `innerText` пуст), самый **короткий** подходящий контейнер, а не самый длинный (они вложены, внешний тащит доставку и отзывы); `og:description` переехал в конец цепочки. Материал — `specValue` по таблице (шесть языков; `\b` заменён на lookahead с флагом `u`, иначе `^склад\b` не совпадает с «Склад») и `compositionFromText` по тексту: состав обрывается на 100% (дальше подкладка), волокно — одно слово или два, если дальше конец оборота, «Extra 20% off» не волокно. Бренд — плюс таблица и размеченный на странице дизайнер. Крошки — `BreadcrumbList` из разметки (переживает strip, работает и для вставленной страницы) и отрисованная навигация; из них категория и `matchSubcategoryLabel` в `categories.ts` (совпадение по словам, обе стороны разворачиваются в формы: «accessories» → accessory, «hoodies» → hoodie, «dress» не трогается), ярлык дерева важнее совпадения по ключевому слову, `isBuiltInBucket` не даёт принять чужую корзину, `resolveSubcategory` снимает ярлык, противоречащий категории. Стиль — `inferStyleKeywords` по словарю из 13 стилей (имя, описание, материал, ярлык, крошки), не больше трёх, цвет и название вещи в правилах отсутствуют намеренно. Группировка цветов — `variant-group.ts`: сначала точное совпадение по ссылкам цветового ряда (`source_url`), и только если магазин переключает цвет скриптом — бренд + **точное** базовое имя (префиксное совпадение из SQL-запроса отсеивается в коде: «Wool Coat» и «Wool Coat Long» — две вещи), цвета должны различаться, группа из одного не пишется; ссылки ряда фильтруются тем же `looksLikeProductPath`, что у планировщика, и только свой хост. Тот же товар на других сайтах — `same-item.ts` + миграция 020: GTIN с проверкой контрольной цифры GS1, бренд+MPN, **SKU не сшивает магазины** (у разных ритейлеров одна строка означает разные вещи); при совпадении импортёр не вставляет строку, а дописывает магазин в `retailers`, расширяет вилку цены в сторону дешёвого и заполняет **только пустые** поля. Заодно переписан разбор строки характеристик: блок из текстовых узлов рисуется одной строкой, значение съедало следующие строки — теперь значение кончается там, где начинается следующая метка, а ключ отдаётся в одно-, двух- и трёхсловной форме. **Доказательство:** 150/150 юнитов на настоящих модулях (`verification/test-fields.js`) и 56/56 сквозным прогоном настоящего расширения в настоящем Chromium — было 40/40: размеры сняты с кнопок и отдельно из `hasVariant`; цвет — со свотча на странице, где о цвете не сказано нигде; два соседних цвета из ссылок ряда, `Care instructions` из того же ряда отброшен; описание вынуто из свёрнутого аккордеона; материал из `<dl>` и отдельно из одной строки украинского текста; крошки с отрисованной навигации; товар лёг и в категорию, и в подкатегорию («Bomber Jackets»); бренд со ссылки дизайнера там, где JSON-LD бренда не знает; EAN-13 с оффера, артикул с украинской строки; стиль выведен из слов, существовавших только в скрытом описании. `tsc --noEmit` чист, `npm run build` exit 0, база линта не выросла (13/52), `eslint extension` чист | `src/lib/server/product-fields.ts`, `src/lib/categories.ts`, `src/lib/style-keywords.ts`, `src/lib/server/parser/{extract,normalize,types,import-product,variant-group,same-item}.ts`, `src/lib/{types,supabase}.ts`, `src/lib/data/db.ts`, `src/app/api/admin/parser/collect/route.ts`, `src/app/goo-studio/parser/collect/page.tsx`, `extension/{snapshot,background}.js`, `supabase/migrations/020_product_codes.sql` | Закрыто в коде. **Требует шага CEO:** миграции 019 и 020 (без 020 сшивки между магазинами не будет — появится второй товар вместо ссылки). **Не проверено мной:** живая база, поэтому группировка цветов и сшивка товара проверены как чистые функции, а запросы вокруг них нет; живой магазин. **Осталось:** название товара (`og:title` тащит имя магазина, `<h1>` парсер не читает) — по решению CEO отложено |
| 2026-09-23 | **Сбор расширением выбрасывал всё, что прислал, и название снова тащило имя магазина.** По прямому указанию CEO (вне порядка волн, деньги не задействованы). **Найдено по коду:** JSON-LD `ProductGroup` (одна вещь, варианты под `hasVariant`) считался листингом из N товаров — `parsePage` уходил в ветку листинга, брал первую карточку JSON-LD и терял фото, размеры, цвет, описание и валюту со страницы; импорт не обновлял `price_min_usd`, поэтому фильтр «до $200» видел неконвертированные 200 €; правка названия (`74b943f`: `<h1>`, `tidyProductName`, `commonTitleSuffix`, `document.title` из расширения) потеряна при слиянии `5d7f379` (#867) — в `src` не было ни одного вхождения. **Сделано.** Внешний патч ProductGroup применён как есть (группа читается как одна вещь, цвет/GTIN с показанного варианта, фото и размеры только этого цвета, другие цвета → `variantUrls`; `isVariantOf` дозаполняет вариант; `isSimilarTo`/`isRelatedTo` — чужие товары; двойное описание одного товара сливается; страница с `evidence` всегда один товар; `price_min_usd`/`price_max_usd` пишутся при импорте и сшивке, колонки в `OPTIONAL_COLUMNS`; иконки в `manifest.json`). Правка названия перенесена руками поверх текущей схемы `evidence` (без revert, #867 не тронут), `61a4c31` не возвращался. **Отличие от `74b943f`:** оригинал склеивал оставшиеся части через « — », то есть «Nebula Jacket - Black» становился «Nebula Jacket — Black», а группировка цветов из #867 (`variantBaseName` → `getBaseProductName`) режет цвет только по « - » — чёрная и бежевая куртка перестали бы группироваться по имени; теперь разделители, которые не пришлось убрать, остаются как у магазина. Миграции: корневой `supabase-migration-color-groups.sql` в SQL Editor не проходит вовсе (`create policy if not exists` — не синтаксис PostgreSQL, скрипт уходит одним запросом и отклоняется целиком), а его `on conflict do nothing` без уникального ключа задваивает цвета при повторе — добавлена идемпотентная `021_color_groups.sql`. **Доказательство:** `tsc --noEmit` exit 0, `npm run build` exit 0, `eslint extension` exit 0, общий линт 11 ошибок / 51 предупреждение до и после (ни один файл не изменил счёт; в задании CEO стояло 13/52 — это число с более ранней точки, на `0df594b` фактически 11/51); проверки на настоящих модулях: ProductGroup 33/33, поля 150/150, цена и фото 47/47, серверная логика 39/39, название 23/23 (заголовок «Куртка бомбер, чёрная — MyStore \| Купить с доставкой» → «Куртка бомбер, чёрная»; `<h1>` выше `og:title`, логотип-`h1` пропущен, JSON-LD выше шумного `h1`, две страницы ещё не дают хвоста, три — дают, хвост магазина снят через `parsePage`; «Nebula Jacket - Black \| MyStore» и «… - Camel \| MyStore» остаются сиблингами по имени). Миграции 019, 019, 020, 021 прогнаны на живом PostgreSQL 16 + pgvector поверх `supabase-schema.sql` и 001–018, каждым файлом одним запросом, как шлёт SQL Editor, дважды подряд, в двух состояниях (нет `color_groups` / уже засеяна): все OK, 13 цветов без дублей, одна политика, все 10 колонок, обе RPC; строка в форме нового импорта (EUR→USD, GTIN, `color_group_ids`) принята, кривой GTIN отклонён ограничением | `src/lib/server/parser/{extract,parse-page,normalize,types,import-product}.ts`, `src/lib/server/product-fields.ts`, `src/lib/data/db.ts`, `src/app/api/admin/parser/collect/route.ts`, `src/app/goo-studio/parser/collect/page.tsx`, `extension/{manifest.json,snapshot.js,background.js}`, `supabase/migrations/021_color_groups.sql` | Закрыто в коде. **Требует шага CEO:** миграции 019_product_price_usd → 019_product_source_price → 020_product_codes → 021_color_groups (записей о прогоне в прод нет, все идемпотентны), слияние PR в `master` (среда агента пушит только в свою ветку), обновление расширения и повторный сбор. **Не проверено мной:** живая база и живой магазин; `graphify update` не выполнялся — CLI в контейнере нет |

---

## 8. Доска статусов

Статусы: `[ ]` не начато · `[~]` в работе · `[x]` закрыто и проверено по DoD · `[н/а]` неактуально (проблемы нет) · `[!]` заблокировано, нужен CEO.

В колонку «доказательство» — одна строка: что именно проверено. Не «сделано».

### Волна 1 — разблокировать оплату

| | ID | Задача | Дата | Доказательство |
|---|---|---|---|---|
| `[x]` | Б0-1 | `/sign-in` → `/login` в двух файлах | 2026-08-07 | Прод подтвердил проблему: `goo-fashion.com/sign-in` → 404, `/login` → 200. Обе ссылки переведены; `redirectUrl` заменён на `fallbackRedirectUrl` (иначе Clerk игнорирует `redirect_url` и уводит на главную); eslint-правило ловит возврат `/sign-in` — проверено пробным файлом |
| `[x]` | Б0-2 | Настоящая страница `/register` + вход в навигации | 2026-08-07 | Прод подтвердил проблему: `/register` → 307 → главная. Локально `/register` отдаёт 200 без редиректа, в HTML присутствует `SignUp`; «Sign up» добавлен в десктопный и мобильный блок `SignedOut`; тема Clerk переведена на переменные сайта. Живой вид карточки Clerk не снят — в контейнере нет боевых ключей Clerk |
| `[~]` | Б0-3 | Изображения каталога: убрать 429 | 2026-08-09 | **Причина в плане названа неверно** (замер 2026-08-07 по DoD `/_next/image?url=…&w=640&q=75`): Farfetch 6/6 → 429, productserve 6/6 → 200, Supabase 6/6 → 200, на `w=3840` та же картина — квота **не** исчерпана, 429 привязан к хосту `cdn-images.farfetch-contents.com`, тот же файл напрямую с CDN → 200. Подтверждение механизма в коде: `api/admin/image-tools/route.ts:33-56` уже обходит эту же защиту браузерным UA + `Referer`, чего оптимизатор не умеет. CEO выбрал вариант **(в) гибрид** (2026-08-09). Сделано: `shouldOptimizeImage()` (`src/lib/image.ts`) + обёртка `src/components/ui/Image.tsx`, через неё переведены все 18 файлов с `next/image`; `remotePatterns` с `**` сужены до своих хостов (заодно закрыт открытый прокси `/_next/image?url=<любой адрес>`); eslint-правило запрещает прямой импорт `next/image`. Проверено фактически: политика на 13 формах URL — 13/13; `tsc --noEmit` — 0 ошибок; `npm run build` — exit 0; линт на базе `da86e8d` и с изменением одинаков (24 ошибки / 53 предупреждения — все чужие, по моим файлам ноль). Живые запросы к поднятому продовому серверу: `/_next/image?url=…&w=640&q=75` для Farfetch, productserve и произвольного чужого адреса → **400** (до всякой загрузки), для `/public` → **200**, для `*.supabase.co` → попытка загрузки (`getaddrinfo ENOTFOUND` на фейковом проекте), то есть свои хосты оптимизатор по-прежнему обслуживает. В пререндеренном HTML: внешняя картинка выходит как `<img referrerPolicy="no-referrer" src="https://images.unsplash.com/…">` без `/_next/image`, а все 256 обращений к оптимизатору на главной — свои `/cs/*.png`. **Расхождение с планом:** 429 ловили не только `/browse` и карточка — те же внешние URL рендерит главная (`HeroProductCycle`, `FeaturesBento`, `AIStylistShowcase`) и админка, все переведены. **Осталось для закрытия:** на проде подтвердить, что партнёрские CDN отдают 200 самому браузеру (в контейнере нет сети наружу: гейт 403 на `goo-fashion.com`, внешние хосты 000). 429 от оптимизатора структурно невозможен — внешние URL до него больше не доходят |
| `[!]` | Б0-4 | Диагностика подписок в Supabase (не код) | 2026-08-07 | Нужен доступ к Supabase — в контейнере его нет. Два SQL-запроса из плана готовы к прогону, результат определяет, какая из задач Б1-1/Б1-2/Б1-4 первая |
| `[!]` | Б0-5 | `CRON_SECRET` задан в Vercel | 2026-08-07 | Нужен доступ к переменным окружения Vercel — в контейнере его нет. Проверено по коду: `renew/route.ts:30` действительно возвращает 401 при незаданной переменной |
| `[x]` | Б5-1 | Закрыть `/api/report-bug` авторизацией и лимитом | 2026-08-07 | Живые прогоны: аноним → 401; >4000 симв. описания и >5 МБ base64 → 413; принудительный отказ лимитера → 429 + `Retry-After: 900`; без `ANTHROPIC_API_KEY` → 503 вместо 500. Лимит 5/час на userId, отдельная корзина Redis. **Не проверено:** реальный счёт до 429 — в контейнере нет Upstash |

### Волна 2 — за что платят и чем меряем

| | ID | Задача | Дата | Доказательство |
|---|---|---|---|---|
| `[x]` | Б1-1 | `activateSubscription()` проверяет результат записи | 2026-08-07 | Прогон настоящего `activateSubscription` через настоящий `supabase-js` против локального PostgREST-совместимого сервера, три сценария: (A) строки нет → строка создана целиком (`status=active`, `amount=39900`, `ccy=980`, токен карты), `auto_renew` в payload отсутствует, падение дальше на «Missing Clerk Secret Key» — то есть база записалась **до** Clerk; (B) таблицы нет → throw + `console.error` + событие `ledger_error` со `status: "error"`; (C) 0 строк без ошибки (исходный баг) → тот же явный отказ вместо тишины. **Не проверено:** применена ли миграция `supabase-migration-subscriptions.sql` в проде — нужен доступ (п.2 задачи) |
| `[x]` | Б1-2 | Не терять токен карты | 2026-08-07 | Два живых прогона через заглушки PostgREST + API monobank (`MONOBANK_API_BASE` конфигурируем). **Вебхук** с настоящей проверкой подписи ECDSA: с картой → токен на строке, без карты → строка записана, событие `card_token_missing` со `status: "warning"` и причиной + строка в консоли. **Cron**: подписка без токена, которой 40 ч, → токен подтянут (`tok_recovered`, masked pan), событие `card_token_recovered`, и она же списана **в том же прогоне**; вторая, у которой карты в кошельке нет, → `card_token_missing` со `status: "error"` (старше суток). В админке: счётчик `activeWithoutCard`, красный баннер и «no card» в колонке карты |
| `[x]` | Б1-3 | Перестать глушить журнал платежей | 2026-08-07 | Живые прогоны: нет таблицы `billing_events` → `console.error` с самим событием (`payment_success/initial user=u2 invoice=inv_a`), исключение не выброшено; Supabase не настроен → то же самое; due-подписка без токена в cron → событие `renewal_skipped` (`status: warning`, причина) и `outcome: "skipped"` вместо немого `continue`. **Расхождение с планом:** глушил не `catch {}` — PostgREST отдаёт ошибку в поле `error`, не бросая, поэтому отсутствие таблицы не ловилось вообще. **Не проверено:** применена ли миграция в проде — нужен доступ |
| `[x]` | Б1-4 | Алерты по биллингу | 2026-08-07 | Шесть живых прогонов через настоящий SDK Resend, перенаправленный на локальный сервер (`RESEND_BASE_URL`), — письмо ловится целиком: (1) `CRON_SECRET` не задан → 401 + событие `cron_misconfigured` + письмо с инструкцией по починке, повтор в течение 12 ч подавлен; (2) последний прогон 40 ч назад → «Renewal cron had a 40h gap»; (3) есть due, ноль продлений → письмо со списком конкретных отказов; (4) `payment_failed` в вебхуке → письмо с причиной от monobank; (5) недельная сводка (`getUTCDay` подменён на понедельник) → активные 2, past due 1, продлений 1, новых 1, отказов 1, 1398 ₴, отмечена ledger-ошибка; (6) получатели не заданы → письмо не уходит, но целиком печатается в консоль. **Не проверено:** доставка в реальный ящик — нужен боевой `RESEND_API_KEY`. **Ограничение:** полностью не запускающийся cron сам о себе сообщить не может — пропуск виден на следующем состоявшемся прогоне и по пульсу `cron_run` в админке |
| `[x]` | Б1-5 | Состояние биллинга в админке | 2026-08-07 | Прогон настоящего роута `/api/admin/subscriptions` против заглушки БД с заведомо больным состоянием: `activeWithoutCard=1`, `overdue=1`, `failedCharges=5` (2+3), пульса cron нет (`lastCronRunAt=null`), MRR 3197 ₴ — все цифры сошлись с фикстурой. Пофамильно: `user_nocard` без токена, `user_overdue` просрочен с 2 отказами, `user_ok` здоров. В журнале видны все пять типов событий, включая новые `ledger_error` и `card_token_missing`. **`card_token` наружу не уходит** — проверено по ответу API. **Не снято:** скриншот экрана — Chromium в контейнере не достаёт до локального сервера; панель Billing health отрисовывается на клиенте из этого же JSON |
| `[ ]` | Б2-1 | Гейт AI-стилиста с бесплатной воронкой | | |
| `[ ]` | Б2-2 | Гейт сохранения образов (Pro) | | |
| `[ ]` | Б2-3 | Месячная квота генераций + rate-limit | | |
| `[ ]` | Б2-4 | Лимиты стилиста — месячные, потолок для Premium | | |
| `[ ]` | Б2-5 | Реализовать или убрать функции Premium | | |
| `[ ]` | Б3-1 | `/plans` = код по всем восьми строкам | | |
| `[ ]` | Б3-2 | Честная валюта (гривна основная) | | |
| `[ ]` | Б3-3 | Сетка тарифов | | |
| `[ ]` | Б4-1 | Починить Vercel Analytics (404 на скриптах) | | |
| `[ ]` | Б4-2 | `track()` по всей воронке (14 событий) | | |
| `[ ]` | Б4-3 | Дашборд воронки в админке | | |
| `[ ]` | Б5-2 | Rate-limit на `/api/waitlist` и `/api/analytics/event` | | |
| `[ ]` | Б5-3 | Не светить внутренний адрес в редиректах | | |

### Волна 3 — рост

| | ID | Задача | Дата | Доказательство |
|---|---|---|---|---|
| `[ ]` | Б6-1 | Партнёрка Farfetch (118 ссылок без метки) | | |
| `[ ]` | Б6-2 | Трекинг кликов «Where to buy» | | |
| `[ ]` | Б6-3 | Отчёт по партнёрским доходам | | |
| `[ ]` | Б7-1 | Ценностное предложение на главной | | |
| `[ ]` | Б7-2 | H1 и уникальные метаданные | | |
| `[ ]` | Б7-3 | Язык и рынок (`lang`, hreflang) | | |
| `[ ]` | Б7-4 | Гидратация React #418 на 9 маршрутах | | |
| `[ ]` | Б7-5 | Поведенческие раздражители (скролл, всплывашка, корзина, два входа) | | |
| `[ ]` | Б7-6 | og:image для блога | | |
| `[ ]` | Б9-1 | Lifecycle-письма, включая dunning | | |
| `[ ]` | Б9-2 | Причина вернуться на второй день | | |
| `[ ]` | Б10-1 | Похудеть builder (4,9 МБ на мобильном) | | |
| `[ ]` | Б10-2 | Размеры элементов на мобильном | | |
| `[ ]` | Б10-3 | `/sitemap-page`: горизонтальный скролл | | |

### Волна 4 — каталог и модель

| | ID | Задача | Дата | Доказательство |
|---|---|---|---|---|
| `[ ]` | Б8-1 | `styleKeywords` для 93% каталога | | |
| `[ ]` | Б8-2 | Женский сегмент (сейчас 3 товара из 317) | | |
| `[ ]` | Б8-3 | Описания товаров | | |
| `[ ]` | Б8-4 | Образы до 9+ и регулярность блога | | |
| `[ ]` | Б8-5 | Ценовое позиционирование | | |
| `[ ]` | Б11 | Решение о модели монетизации (CEO) | | |

---

## 9. Эскалация к CEO

### Когда останавливаться

**Обязательная остановка — конец каждой волны.** Закрыта последняя задача волны → не начинай следующую, готовь отчёт. Это точка, где CEO смотрит на результат и подтверждает или меняет курс.

**Останавливаться немедленно, не дожидаясь конца волны, если:**
- дошёл до развилки из раздела 6;
- задача требует денег, доступов или решения вне кода;
- нашёл проблему серьёзнее той, над которой работаешь;
- план разошёлся с кодом настолько, что задача потеряла смысл.

**Когда закрыт весь план (Б0–Б10):** это конец текущего плана, а не конец работы. Готовь финальный отчёт и **запрашивай у CEO новый план**. Сам новый план не пиши и не выдумывай следующие задачи — приоритеты после закрытия аудита определяет CEO, опираясь на данные, которых на момент написания этого плана не существовало.

### Форма отчёта

```
ОТЧЁТ ПО ВОЛНЕ N — <дата>

1. ЧТО ЗАКРЫТО
   По каждой задаче: ID, одна строка результата, чем доказано.

2. ЧТО НЕ ЗАКРЫТО И ПОЧЕМУ
   ID, причина, что нужно для разблокировки. Без смягчений.

3. РАСХОЖДЕНИЯ С ПЛАНОМ
   Где план не совпал с кодом. Что оказалось проще, что сложнее оценки.

4. ЧИСЛА
   Метрики из раздела «Метрики» плана: было → стало.
   Отдельно: появились ли платящие, сколько регистраций, себестоимость AI.

5. НАЙДЕНО СВЕРХ ПЛАНА
   Из раздела 7 этого файла.

6. РЕШЕНИЯ, КОТОРЫЕ НУЖНЫ ОТ ТЕБЯ
   Развилки из раздела 6: варианты, цена вопроса, рекомендация.

7. ЗАПРОС
   Волна N закрыта. Прошу подтвердить переход к волне N+1
   / прошу новый план — текущий исчерпан.
```

### Тон отчёта

Коротко, фактами, без украшений. CEO нужно за минуту понять три вещи: что теперь работает, что нет, и какое решение от него требуется. Плохие новости — первыми и прямо: «продление всё ещё не работает, причина в X» полезнее, чем абзац про проделанную работу.

Не подгоняй результат под ожидание. Если после волны 2 платящих не появилось — так и напиши, и предложи, где искать причину.
