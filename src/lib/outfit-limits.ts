/**
 * Most pieces an outfit takes — as many as the collage on the site can draw.
 * The studio's editor stops at it and the outfits API refuses more, so the two
 * read the same number from here.
 */
export const MAX_OUTFIT_ITEMS = 6;
